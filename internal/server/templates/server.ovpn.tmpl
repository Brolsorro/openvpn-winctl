# Generated {{.Timestamp}}
# DO NOT EDIT — managed by ovpnctl. Use "ovpnctl config update" to change settings.

port {{.Port}}
proto {{.Proto}}
dev tun
{{if .WindowsDriver}}windows-driver {{.WindowsDriver}}
{{end}}{{if .NoCompression}}allow-compression no
{{end}}
ca "{{.ConfigDir}}/ca.crt"
cert "{{.ConfigDir}}/server.crt"
key "{{.ConfigDir}}/server.key"
dh "{{.ConfigDir}}/dh.pem"
{{.TLSLine}}
{{if .CRLLine}}{{.CRLLine}}
{{end}}
server {{.Network}} {{.Netmask}}
topology {{.Topology}}
{{if .PersistKey}}persist-key
{{end}}persist-tun

cipher {{.Cipher}}
{{if .DataCiphers}}data-ciphers {{.DataCiphers}}
{{end}}{{if .Auth}}auth {{.Auth}}
{{end}}keepalive {{.Keepalive}}

{{range .PushRoutes}}push "{{.}}"
{{end}}{{range .ExtraPushRoutes}}push "{{.}}"
{{end}}
# When running as a Windows service, OpenVPN writes to:
# C:\ProgramData\OpenVPN\Log\server.log
status "{{.StatusLog}}"
verb {{.Verb}}
{{if .RDPEnabled}}
# RDP/performance optimizations
# NOTE: fast-io is omitted — it disables DCO in OpenVPN 2.6+
sndbuf {{.SndBuf}}
rcvbuf {{.RcvBuf}}
push "sndbuf {{.SndBuf}}"
push "rcvbuf {{.RcvBuf}}"
tun-mtu {{.TunMTU}}
mssfix {{.MSSFix}}
{{if .FastIO}}fast-io
{{end}}{{end}}
